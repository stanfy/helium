buildscript {
  repositories {
    mavenLocal()
    mavenCentral()
    maven { url 'https://oss.sonatype.org/content/repositories/snapshots/' }
  }
  dependencies {
    classpath 'com.stanfy.helium:gradle-plugin:0.3.6'
  }
}

apply plugin: 'helium'

helium {
  specification file('twitter.api')
}

task check()
afterEvaluate {
  check.dependsOn 'genApiTests'
}
