package com.stanfy.helium.handler.codegen.json.schema;

import com.stanfy.helium.handler.codegen.GeneratorOptions;

/**
 * Json schema generator options (just stub for now).
 */
public class JsonSchemaGeneratorOptions extends GeneratorOptions {

  public static JsonSchemaGeneratorOptions defaultOptions() {
    return new JsonSchemaGeneratorOptions();
  }

}
