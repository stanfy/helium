extension {{name}}: Decodable {
    public static func decode(json: AnyObject) throws -> {{name}} {
        return try {{name}}(
            {{#props}}
              {{{definition}}}
            {{/props}}
        )
    }
}

